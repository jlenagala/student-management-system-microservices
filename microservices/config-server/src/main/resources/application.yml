server:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jlenagala/student-management-system-microservices-config-store
          search-paths:
            - 'service-config/*service'